<Window x:Class="PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Symulacja Kulek" Height="550" Width="800"
        SizeChanged="Window_SizeChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" Background="#F0F0F0">
            <TextBlock Text="Liczba kul:" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" 
         Width="50" VerticalContentAlignment="Center" Margin="5"/>

            <Button Content="Start" Command="{Binding StartCommand}" Margin="5" Padding="10,5"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="5" Padding="10,5"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ItemsControl x:Name="BallsControl" ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="White" IsItemsHost="True" ClipToBounds="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Fill="RoyalBlue" Width="{Binding Radius}" Height="{Binding Radius}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</Window>