<Window x:Class="PresentationView2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationView2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="150"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<StackPanel Grid.Column="0" Margin="10" Background="#F8F8F8">
			<Label Content="Liczba kul:" Margin="0,10,0,0" FontWeight="Bold"/>
			<TextBox Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged}"
                     Margin="5" Height="25" VerticalContentAlignment="Center"/>

			<Button Content="Start Simulation"
                    Command="{Binding StartCommand}"
                    Margin="5,20,5,5" Height="35" Cursor="Hand"/>
		</StackPanel>

		<Border Grid.Column="1" BorderBrush="Gray" BorderThickness="2" Margin="10" Background="White">
			<ItemsControl ItemsSource="{Binding Balls}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<Canvas IsItemsHost="True" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>

				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Ellipse Fill="RoyalBlue"
                                 Width="{Binding Radius}"
                                 Height="{Binding Radius}">
							<Ellipse.RenderTransform>
								<TranslateTransform X="{Binding X}" Y="{Binding Y}"/>
							</Ellipse.RenderTransform>
						</Ellipse>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Border>
	</Grid>
</Window>
